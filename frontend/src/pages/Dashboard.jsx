function Dashboard() {
  return (
    <div style={{ maxWidth: 800, margin: "50px auto" }}>
      <h2>Dashboard</h2>
      <p>Welcome! This is a placeholder — we'll build the real dashboard next.</p>
    </div>
  );
}

export default Dashboard;